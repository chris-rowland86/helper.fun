% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pptx_helpers.R
\name{add_to_pptx}
\alias{add_to_pptx}
\title{Add RVG Graphic to PowerPoint Slide}
\usage{
add_to_pptx(
  pptx,
  rvg_graphic,
  title = "Title",
  title_lines = "one",
  top = 1.5,
  left = 0.3,
  fig_width = 10,
  fig_height = 5,
  layout_one = "Title and Content_No Graphic",
  layout_two = "Title Two Line and Content_No Graphic",
  master = "Office Theme",
  title_color = "#003494",
  title_size = 36,
  title_family = "Arial"
)
}
\arguments{
\item{pptx}{An officer pptx object (the PowerPoint presentation)}

\item{rvg_graphic}{An rvg dml object created by \code{convert_to_rvg()}}

\item{title}{Character string for the slide title. Defaults to "Title"}

\item{title_lines}{Character indicating title layout: "one" for single line,
"two" for two-line title layout. Defaults to "one"}

\item{top}{Numeric specifying the top position of the figure in inches.
Defaults to 1.5}

\item{left}{Numeric specifying the left position of the figure in inches.
Defaults to 0.3}

\item{fig_width}{Numeric specifying the figure width in inches.
Defaults to 10}

\item{fig_height}{Numeric specifying the figure height in inches.
Defaults to 5}

\item{layout_one}{Character string for single-line title layout name.
Defaults to "Title and Content_No Graphic"}

\item{layout_two}{Character string for two-line title layout name.
Defaults to "Title Two Line and Content_No Graphic"}

\item{master}{Character string for slide master name.
Defaults to "Office Theme"}

\item{title_color}{Hex color code for title text.
Defaults to TG royal blue ("#003494")}

\item{title_size}{Numeric font size for title. Defaults to 36}

\item{title_family}{Font family for title. Defaults to "Arial"}
}
\value{
The modified pptx object with the new slide added
}
\description{
Adds an rvg graphic to a PowerPoint presentation slide. This function
adds a new slide with the specified layout and inserts the graphic
along with a formatted title.
}
\examples{
\dontrun{
library(ggplot2)
library(officer)

# Create presentation
pptx <- read_pptx("template.pptx")

# Create plot and convert to rvg
p <- ggplot(mtcars, aes(x = mpg, y = hp)) +
  geom_point()
rvg_plot <- convert_to_rvg(p)

# Add to presentation
pptx <- add_to_pptx(pptx, rvg_plot, title = "MPG vs Horsepower")

# Save presentation
print(pptx, target = "output.pptx")
}

}
